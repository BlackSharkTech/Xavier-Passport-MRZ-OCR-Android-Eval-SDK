<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xavier.blacksharktech.com.xavierlibrary" >

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:screenOrientation="landscape"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="xavier.blacksharktech.com.xavierlibrary.MainActivity"
            android:background="@color/white"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:label="@string/activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="xavier.blacksharktech.com.xavierlibrary.DocumentTypeSelectionActivity"
            android:label="@string/title_activity_document_type_selection" >
        </activity>

    </application>
</manifest>